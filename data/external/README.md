# External Data Directory

This directory contains external reference files used to supplement the transactional data:
1. `country_codes.csv` - Mapping of Country names to standard ISO 3166-1 alpha-3 codes (for geographic visualizations).
2. `exchange_rates.json` - Historical conversion rates if multi-currency tracking is enabled.

All files in this directory should be static lookup tables or raw external reference files.
